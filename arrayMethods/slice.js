//Array method
//slice method

// syntax: Array.slice(startindex,lastindex(excluded))

let nums=[1,2,3,5,8,6,2];
let result=nums.slice(1,4);

// prompt("original array:",nums);
// prompt("extracted array:",result);
